<?xml version="1.0" encoding="UTF-8"?>
<!--
  Logback config with profile-aware appenders.

    dev profile     -> colored console pattern, single line per event
    prod profile    -> structured JSON to stdout (one event per line) so
                       container log drivers (Docker JSON file driver,
                       fluentd, vector, etc.) can parse without further
                       processing

  Active profile is picked up from spring.profiles.active /
  SPRING_PROFILES_ACTIVE. The 'default' profile inherits from dev.
-->
<configuration>

    <include resource="org/springframework/boot/logging/logback/defaults.xml"/>

    <property name="LOG_PATH" value="${LOG_PATH:-./logs}"/>
    <property name="LOG_FILE" value="${LOG_FILE:-rag.log}"/>

    <!-- ========================================================== -->
    <!-- DEV / DEFAULT: human-friendly console output                -->
    <!-- ========================================================== -->
    <springProfile name="dev | default">
        <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>${CONSOLE_LOG_PATTERN}</pattern>
                <charset>UTF-8</charset>
            </encoder>
        </appender>

        <root level="INFO">
            <appender-ref ref="CONSOLE"/>
        </root>
    </springProfile>

    <!-- ========================================================== -->
    <!-- PROD: structured JSON to stdout + rolling file              -->
    <!-- ========================================================== -->
    <springProfile name="prod">
        <appender name="JSON_CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeContext>false</includeContext>
                <includeMdc>true</includeMdc>
                <fieldNames>
                    <timestamp>@timestamp</timestamp>
                    <message>message</message>
                    <thread>thread</thread>
                    <logger>logger</logger>
                    <level>level</level>
                    <stackTrace>stack_trace</stackTrace>
                </fieldNames>
                <customFields>{"app":"rag","env":"${APP_ENV:-prod}"}</customFields>
            </encoder>
        </appender>

        <appender name="ROLLING_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <file>${LOG_PATH}/${LOG_FILE}</file>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <fileNamePattern>${LOG_PATH}/${LOG_FILE}.%d{yyyy-MM-dd}.%i.gz</fileNamePattern>
                <maxFileSize>100MB</maxFileSize>
                <maxHistory>14</maxHistory>
                <totalSizeCap>2GB</totalSizeCap>
            </rollingPolicy>
            <encoder class="net.logstash.logback.encoder.LogstashEncoder">
                <includeMdc>true</includeMdc>
                <customFields>{"app":"rag","env":"${APP_ENV:-prod}"}</customFields>
            </encoder>
        </appender>

        <!-- Async wrapper: don't let stdout backpressure stall request threads -->
        <appender name="ASYNC_JSON" class="ch.qos.logback.classic.AsyncAppender">
            <queueSize>1024</queueSize>
            <discardingThreshold>0</discardingThreshold>
            <neverBlock>true</neverBlock>
            <appender-ref ref="JSON_CONSOLE"/>
        </appender>

        <root level="WARN">
            <appender-ref ref="ASYNC_JSON"/>
            <appender-ref ref="ROLLING_FILE"/>
        </root>

        <logger name="com.majm.rag" level="INFO"/>
        <logger name="org.springframework.web" level="INFO"/>
        <logger name="org.springframework.kafka" level="INFO"/>
    </springProfile>

</configuration>
